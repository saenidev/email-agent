{
  "name": "email-agent",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo run dev",
    "dev:all": "concurrently -n api,web,worker -c blue,green,yellow \"cd apps/api && uv run uvicorn app.main:app --reload --port 8001\" \"pnpm --filter @email-agent/web dev\" \"cd apps/api && uv run arq app.workers.worker.WorkerSettings\"",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.3.3"
  }
}
